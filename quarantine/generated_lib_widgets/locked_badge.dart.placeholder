import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class LockedBadge extends StatelessWidget {
  const LockedBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'Locked Badge',
      child: const Center(
        child: Text('Locked Badge'),
      ),
    );
  }
}
