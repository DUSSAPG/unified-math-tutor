class PracticeContext {
  final String stage;
  final String questionText;
  final String topic;

  const PracticeContext({
    required this.stage,
    required this.questionText,
    required this.topic,
  });
}
