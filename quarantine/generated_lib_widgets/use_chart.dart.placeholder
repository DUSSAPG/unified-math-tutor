import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class UseChart extends StatelessWidget {
  const UseChart({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'use Chart',
      child: const Center(
        child: Text('use Chart'),
      ),
    );
  }
}
