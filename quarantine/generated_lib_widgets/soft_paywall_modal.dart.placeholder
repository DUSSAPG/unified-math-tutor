import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class SoftPaywallModal extends StatelessWidget {
  const SoftPaywallModal({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'Soft Paywall Modal',
      child: const Center(
        child: Text('Soft Paywall Modal'),
      ),
    );
  }
}
