import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class SubscriptionProvider extends StatelessWidget {
  const SubscriptionProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'Subscription Provider',
      child: const Center(
        child: Text('Subscription Provider'),
      ),
    );
  }
}
