import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class ProgressProvider extends StatelessWidget {
  const ProgressProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'Progress Provider',
      child: const Center(
        child: Text('Progress Provider'),
      ),
    );
  }
}
