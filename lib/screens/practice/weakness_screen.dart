import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class WeaknessScreen extends StatelessWidget {
  const WeaknessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'Weakness Screen',
      child: const Center(
        child: Text('Weakness Screen'),
      ),
    );
  }
}
