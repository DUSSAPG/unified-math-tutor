import 'package:flutter/material.dart';
import 'package:flutter_shared_ui/flutter_shared_ui.dart';

class RCChecklist extends StatelessWidget {
  const RCChecklist({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'RCChecklist',
      child: const Center(
        child: Text('RCChecklist'),
      ),
    );
  }
}
